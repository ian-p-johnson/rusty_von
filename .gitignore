# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
checkpoints/
data/
.pytest_cache/
src/von/local_backends/
js/node_modules/

# Stage 2: exported ONNX artifact (~1.7 GB, regenerable via
# von-rs/scripts/export_onnx.py from the pinned HF revision).
von-rs/artifacts/

_vizdoom.ini
data_long/

# Personal working notes - never publish.
# Patterned rather than named: the two that leaked (SOTA.md,
# CONTEXT_EXPANSION.md) were committed by a broad `git add` before anyone
# decided they were public, so block the shape, not just the filenames.
SOTA.md
CONTEXT_EXPANSION.md
VON12_PROPOSAL.md
VON_NEXT.md
PROGRESS.md
*_PROPOSAL.md
*_NOTES.md
NOTES.md
SCRATCH*.md
DRAFT*.md
private/
.private/

# vendored onnxruntime-gpu wheel (fetch_ort_gpu.sh)
von-rs/third_party/
